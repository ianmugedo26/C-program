// code for if
#include<stdio.h>
int main(){
    int n,i,cube;

    printf("Enter the number :");
    scanf("%d",number);
    printf("number is %d \n",n);

    for(i=1;i<=n;i++){
        //calculating the cube 
        cube=i*i*i;
        printf("the number is %d \n",i);
        printf("the cube %d is %d \n",i,cube);
    }
     
     return 0;
}